# The virtualenv. Never commit this. Recreate it with:
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
.venv/

# Regenerated by run.sh. The full trees are kept under puzzle-task/ and
# warmup-task/, so there is no need to commit the scratch copy.
results/

__pycache__/
*.pyc
